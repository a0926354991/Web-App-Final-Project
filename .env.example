# Gemini API — used for LLM-generated recommendation explanations and other AI features
GEMINI_API_KEY=

# Toggle features
# 1=on, 0=off。沒填 GEMINI_API_KEY 時自動 fallback 到模板,不會壞掉
USE_LLM_EXPLANATION=1

# Model (default: gemini-2.5-flash — 免費額度寬,快速,支援 JSON 輸出)
GEMINI_MODEL=gemini-2.5-flash
